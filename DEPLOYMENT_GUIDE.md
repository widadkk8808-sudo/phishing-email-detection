# دليل النشر - كشف البريد التصيدي بالذكاء الاصطناعي

## 🎯 النشر على Vercel (الأسهل)

### الخطوات:
1. **ارفع الملفات على GitHub:**
   ```bash
   git add api.py working_phishing_detector.py svm_phishing_model.pkl
   git add index.html script.js styles.css
   git add README.md requirements.txt vercel.json
   git commit -m "Initial commit"
   git push origin main
   ```

2. **انشئ حساب على Vercel.com**

3. **اربط مستودع GitHub:**
   - اضغط "Import Project"
   - اختر مستودعك
   - Vercel سيتكفل بالباقي تلقائياً

4. **اضغط Deploy!**
   - الموقع سيكون جاهز خلال دقائق
   - ستحصل على رابط مثل: `https://your-project.vercel.app`

---

## 🎯 النشر على Railway (أقوى للخادم)

### الخطوات:
1. **ارفع الملفات على GitHub** (نفس الخطوات السابقة)

2. **انشئ حساب على Railway.app**

3. **اربط مستودع GitHub:**
   - اضغط "Deploy from GitHub repo"
   - اختر مستودعك

4. **أضف متغيرات البيئة:**
   ```env
   FLASK_ENV=production
   PORT=5000
   ```

5. **Deploy!**
   - ستحصل على رابط API
   - يمكنك تحديث index.html ليستخدم الرابط الجديد

---

## 📋 قائمة الملفات النهائية

### ملفات أساسية (ارفعها):
- `api.py` - خادم Flask
- `working_phishing_detector.py` - نموذج الذكاء الاصطناعي
- `svm_phishing_model.pkl` - النموذج المُدرّب (19KB)
- `index.html` - واجهة المستخدم
- `script.js` - منطق الواجهة
- `styles.css` - التصميم
- `README.md` - شرح المشروع
- `requirements.txt` - متطلبات Python
- `vercel.json` - إعدادات النشر لـ Vech

### ملفات مولدة تلقائياً (لا ترفعها):
- `emails.csv` (485MB) - قاعدة البيانات الأصلية
- `emails_sample.csv` (2.8MB) - عينة البيانات
- `api_log.txt` - ملفات السجل
- `__pycache__/` - ملفات Python المؤقتة

---

## 🎉 النتيجة المتوقعة

بعد النشر ستحصل على:
- ✅ موقع ويب متاح للجميع
- ✅ نموذج ذكاء اصطناعي يعمل بسرعة
- ✅ واجهة مستخدم جذابة
- ✅ مستودع GitHub خفيف (أقل من 5MB)

**لا تحتاج لخادم محلي بعد الآن!**